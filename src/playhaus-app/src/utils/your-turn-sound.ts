import { createSoundEffect } from "@/utils/sound-effect";

// The chime that says a shared board is waiting on you, specifically.
export const playYourTurn = createSoundEffect(require('@/assets/sounds/your-turn.wav'));
