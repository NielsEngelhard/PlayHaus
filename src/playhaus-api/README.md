# Run the API
Commands to execute from the ./playhaus-api directory
```
go build ./cmd/api     # writes api.exe
go run   ./cmd/api     # build + run
```

# Installation
```CLI
go get gorm.io/gorm
go get gorm.io/driver/sqlite
go get github.com/google/uuid
```

