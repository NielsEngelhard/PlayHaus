# Local SQLite database. It is generated by AutoMigrate on first boot, and a
# committed copy goes stale as soon as a model changes -- SQLite cannot alter a
# primary key type, so an old file rejects new rows instead of migrating.
/data/

# Built binaries
/ph-api
/ph-api.exe

*.db